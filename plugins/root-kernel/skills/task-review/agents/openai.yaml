interface:
  display_name: "Task Review"
  short_description: "Run Mulgae review for one complete task diff"
  default_prompt: "Use $root-kernel:task-review to run Mulgae review for one complete task diff."
policy:
  allow_implicit_invocation: false
