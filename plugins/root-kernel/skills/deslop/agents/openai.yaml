interface:
  display_name: "Deslop"
  short_description: "Remove task-introduced AI code slop safely"
  default_prompt: "Use $root-kernel:deslop to clean up task-introduced code slop without changing behavior."
policy:
  allow_implicit_invocation: true
