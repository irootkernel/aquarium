interface:
  display_name: "Epic Handler"
  short_description: "Orchestrate an epic through sequential task goals"
  default_prompt: "Use $aquarium:epic-handler with one roadmap epic to execute, prepare a plan handoff, or resume it."
policy:
  allow_implicit_invocation: false
