interface:
  display_name: "Task Handler"
  short_description: "Strengthen the procedure around one task goal"
  default_prompt: "Use $aquarium:task-handler with a roadmap path and one task ID to strengthen its delivery procedure."
policy:
  allow_implicit_invocation: false
