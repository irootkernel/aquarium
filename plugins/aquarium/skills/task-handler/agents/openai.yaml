interface:
  display_name: "Task Handler"
  short_description: "Strengthen the procedure around one task goal"
  default_prompt: "Use $aquarium:task-handler with one roadmap task to execute, prepare a plan handoff, or resume it."
policy:
  allow_implicit_invocation: false
