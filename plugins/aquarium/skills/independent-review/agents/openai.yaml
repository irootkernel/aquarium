interface:
  display_name: "Independent Review"
  short_description: "Refuse the disabled route or run one selected alternative"
  default_prompt: "Use $aquarium:independent-review to explain its temporary unavailability, or dispatch exactly one explicitly preselected supported Orca or native Codex alternative under that route's own contract; native Codex additionally requires host fresh delegation."
policy:
  allow_implicit_invocation: false
