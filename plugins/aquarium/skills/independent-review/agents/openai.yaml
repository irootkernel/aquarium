interface:
  display_name: "Independent Review"
  short_description: "Review staged, committed, or planned work"
  default_prompt: "Use $aquarium:independent-review to run a static Codex review of an exact Git target."
policy:
  allow_implicit_invocation: false
