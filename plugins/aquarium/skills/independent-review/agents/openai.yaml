interface:
  display_name: "Independent Review"
  short_description: "Run a read-only review with a fresh Codex"
  default_prompt: "Use $aquarium:independent-review to review one epic or task with a fresh Orca Codex."
policy:
  allow_implicit_invocation: false
